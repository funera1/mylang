// 文法

START ::= { COMPOUND }
COMPOUND ::= STATEMENT | STATEMENT COMPOUND
STATEMENT ::= DECLARATION_STATEMENT | ASSIGN_STATEMENT
DECLARATION_STATEMENT ::= INT ID;
ASSIGN_STATEMENT ::= ID ASSIGN NUMBER;
